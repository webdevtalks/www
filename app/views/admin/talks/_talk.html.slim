tr
  td= talk.title
  td= truncate talk.description, length: 50
  td= link_to talk.speaker.name, admin_speaker_path(talk.speaker)
  /td
    /= link_to edit_admin_talk_path(talk) do
      /i.glyphicon.glyphicon-edit
  /td
    /= link_to admin_talk_path(talk), method: :delete, data: {confirm: 'Estás segur@?' } do
      /i.glyphicon.glyphicon-remove
