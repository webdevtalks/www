= form_for [:admin, location], html: { class: 'form-horizontal' } do |f|

  .form-group
    = f.label :city, 'Ciudad', class: 'col-sm-1'
    .col-sm-2
      = f.text_field :city, class: 'form-control'

  .form-group
    = f.label :state, 'Estado', class: 'col-sm-1'
    .col-sm-2
      = f.text_field :state, class: 'form-control'

  .form-group
    = f.label :country, 'País', class: 'col-sm-1'
    .col-sm-2
      = f.text_field :country, class: 'form-control'

  = f.submit "#{location.new_record? ? 'Crear' : 'Guardar'} ubicación", class: 'btn btn-primary'
